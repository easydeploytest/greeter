apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: greeter
  namespace: argocd
spec:
  generators:
    - list:
        elements:
          - env: dev
          - env: prod
  template:
    metadata:
      name: "greeter-{{env}}"
      annotations:
        notifications.argoproj.io/subscribe.on-deployed.ntfy: ''
        notifications.argoproj.io/subscribe.on-health-degraded.ntfy: ''
        notifications.argoproj.io/subscribe.on-sync-failed.ntfy: ''
    spec:
      project: default
      source:
        repoURL: https://github.com/easydeploytest/greeter
        targetRevision: main
        path: chart
        helm:
          valueFiles:
            - ../values-{{env}}.yaml
      destination:
        server: https://kubernetes.default.svc
        namespace: "greeter-{{env}}"
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
